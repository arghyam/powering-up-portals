import React from 'react';

const AdditionalInfo = () => (
  <div>
    <label>Special Requests:</label>
    <textarea placeholder="Enter any special instructions or requests (optional)"></textarea>

    <div>
      <input type="checkbox" /> Reading and reference material (soft copy) provided
    </div>
  </div>
);

export default AdditionalInfo;
